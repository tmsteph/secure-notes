import React, { useEffect, useState } from "react"; import axios from "./services/axiosInstance"; function App() { const [message, setMessage] = useState(""); useEffect(() => { axios.get("/test").then((res) => setMessage(res.data.message)).catch((err) => console.error("Error:", err)); }, []); return (<div className="App"><h1>React + Laravel</h1><p>{message || "Connecting..."}</p></div>); } export default App;
